{
  "name": "color_game",
  "version": "1.0.0",
  "description": "This a color game app written in JQuery,HTML,CSS with SCSS and HAMLpreprocessors. Guess the color form its RGB, RGBA or HEX code.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon --watch index.js --watch public --ext js,html,css index.js"
  },
  "keywords": [
    "color",
    "jquery",
    "game"
  ],
  "author": "Adam Shaffer",
  "license": "MIT",
  "devDependencies": {
    "sass": "^1.22.10"
  },
  "dependencies": {
    "express": "^4.18.2",
    "nodemon": "^2.0.22",
    "dotenv": "16.0.3"
  }
}
